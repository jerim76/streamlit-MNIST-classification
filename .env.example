# Flask Configuration
FLASK_APP=run.py
FLASK_CONFIG=dev # dev, test, or prod
SECRET_KEY=your_very_secret_flask_key_here # CHANGE THIS!
DEBUG=True # Set to False in production

# Database Configuration (Example for SQLite)
DEV_DATABASE_URL=sqlite:///../dev.db # Relative to the instance folder if not absolute
TEST_DATABASE_URL=sqlite:///../test.db # Relative to the instance folder

# Database Configuration (Example for PostgreSQL - comment out if using SQLite)
# DEV_DATABASE_URL=postgresql://user:password@localhost:5432/fundis_dev
# TEST_DATABASE_URL=postgresql://user:password@localhost:5432/fundis_test
# DATABASE_URL=postgresql://user:password@host:port/fundis_prod # For production

# Twilio Configuration (for WhatsApp) - Add later
# TWILIO_ACCOUNT_SID=your_twilio_account_sid
# TWILIO_AUTH_TOKEN=your_twilio_auth_token
# TWILIO_WHATSAPP_NUMBER=whatsapp:+14155238886 # Twilio Sandbox or your number

# M-Pesa Configuration - Add later
# MPESA_CONSUMER_KEY=your_mpesa_consumer_key
# MPESA_CONSUMER_SECRET=your_mpesa_consumer_secret
# MPESA_SHORTCODE=your_mpesa_shortcode
# MPESA_PASSKEY=your_mpesa_passkey
# MPESA_INITIATOR_USERNAME=your_initiator_username # If using B2C or other specific APIs
# MPESA_INITIATOR_PASSWORD=your_initiator_password # Encrypted, handle carefully
MPESA_API_ENVIRONMENT=sandbox # 'sandbox' or 'live'
MPESA_CALLBACK_URL_BASE=https://your_domain_or_ngrok_url.com # Base URL for callback, e.g. https://mydomain.com or http://localhost:5000 if testing locally with ngrok

# Twilio Configuration (for WhatsApp)
TWILIO_ACCOUNT_SID=ACxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
TWILIO_AUTH_TOKEN=your_twilio_auth_token_here
TWILIO_WHATSAPP_NUMBER=whatsapp:+14155238886 # Default Twilio Sandbox number, change if using your own
FUNDIS_BOT_WHATSAPP_NUMBER=whatsapp:+254xxxxxxxxx # Your app's WhatsApp number if different from Twilio sender
TEST_WHATSAPP_RECIPIENT=whatsapp:+2547xxxxxxxx # For sending test messages from the app

# Other application specific settings
# مثلاً ITEMS_PER_PAGE=10 # Now set in config.py default
